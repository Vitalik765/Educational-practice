<Window x:Class="BudgetApp.Windows.ReportsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Отчеты" Height="600" Width="800"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Период" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <DatePicker x:Name="StartDatePicker" Width="150" Margin="0,0,10,0"/>
            <TextBlock Text=" - " VerticalAlignment="Center" Margin="0,0,10,0"/>
            <DatePicker x:Name="EndDatePicker" Width="150" Margin="0,0,10,0"/>
            <Button Content="Применить" Click="ApplyFilter_Click" Background="#4ECDC4" Foreground="White" Padding="10,5"/>
            <Button Content="💾 Экспорт отчета" Click="ExportReport_Click" Background="#F38181" Foreground="White" Padding="10,5" Margin="5,0,0,0"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2">
            <StackPanel>
                <TextBlock Text="Статистика за период" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
                
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Border Grid.Column="0" Background="#E8F5E9" Padding="15" CornerRadius="5" Margin="5">
                        <StackPanel>
                            <TextBlock Text="Доходы" FontSize="12" Opacity="0.7"/>
                            <TextBlock x:Name="PeriodIncomeText" Text="0 ₽" FontSize="20" FontWeight="Bold" Foreground="#4ECDC4"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Grid.Column="1" Background="#FFEBEE" Padding="15" CornerRadius="5" Margin="5">
                        <StackPanel>
                            <TextBlock Text="Расходы" FontSize="12" Opacity="0.7"/>
                            <TextBlock x:Name="PeriodExpenseText" Text="0 ₽" FontSize="20" FontWeight="Bold" Foreground="#FF6B6B"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Grid.Column="2" Background="#E3F2FD" Padding="15" CornerRadius="5" Margin="5">
                        <StackPanel>
                            <TextBlock Text="Баланс" FontSize="12" Opacity="0.7"/>
                            <TextBlock x:Name="PeriodBalanceText" Text="0 ₽" FontSize="20" FontWeight="Bold" Foreground="#2196F3"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <TextBlock Text="Расходы по категориям" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                <ItemsControl x:Name="ExpenseReportList" Margin="0,0,0,20">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                                    Margin="0,0,0,5" Padding="15" CornerRadius="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Key}" VerticalAlignment="Center" FontSize="14"/>
                                    <ProgressBar Grid.Column="1" Value="{Binding Percentage}" Maximum="100" 
                                                 Width="200" Height="20" Margin="10,0" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" Text="{Binding Value, StringFormat='{}{0:N2} ₽'}" 
                                               FontWeight="Bold" VerticalAlignment="Center" MinWidth="100"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Text="Доходы по категориям" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                <ItemsControl x:Name="IncomeReportList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                                    Margin="0,0,0,5" Padding="15" CornerRadius="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Key}" VerticalAlignment="Center" FontSize="14"/>
                                    <ProgressBar Grid.Column="1" Value="{Binding Percentage}" Maximum="100" 
                                                 Width="200" Height="20" Margin="10,0" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" Text="{Binding Value, StringFormat='{}{0:N2} ₽'}" 
                                               FontWeight="Bold" VerticalAlignment="Center" MinWidth="100"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>

