# Приложение для ведения бюджета

Приложение для управления личным бюджетом, написанное на C# с использованием WPF.

## Возможности

- ✅ Добавление и редактирование транзакций (доходы и расходы)
- ✅ Категоризация транзакций
- ✅ Отображение статистики (доходы, расходы, баланс)
- ✅ Статистика по категориям
- ✅ Фильтрация транзакций
- ✅ Отчеты за выбранный период
- ✅ Автоматическое сохранение данных в JSON файл

## Требования

- .NET 8.0 SDK или выше
- Windows (для WPF приложения)

## Установка и запуск

1. Убедитесь, что у вас установлен .NET 8.0 SDK
2. Откройте терминал в папке проекта
3. Выполните команду для восстановления зависимостей:
   ```
   dotnet restore
   ```
4. Запустите приложение:
   ```
   dotnet run
   ```

Или откройте проект в Visual Studio и запустите его оттуда.

## Структура проекта

```
BudgetApp/
├── Models/              # Модели данных
│   ├── Transaction.cs  # Модель транзакции
│   └── Category.cs     # Модель категории
├── Services/           # Бизнес-логика
│   ├── DataService.cs  # Работа с данными (сохранение/загрузка)
│   └── BudgetService.cs # Расчеты и статистика
├── Windows/            # Окна приложения
│   ├── TransactionWindow.xaml # Окно добавления/редактирования транзакции
│   └── ReportsWindow.xaml     # Окно отчетов
├── MainWindow.xaml     # Главное окно
├── App.xaml            # Конфигурация приложения
└── BudgetApp.csproj    # Файл проекта
```

## Использование

### Добавление транзакции

1. Нажмите кнопку "+ Добавить транзакцию"
2. Выберите тип (Доход или Расход)
3. Введите сумму
4. Введите описание
5. Выберите дату
6. Выберите категорию
7. Нажмите "Сохранить"

### Редактирование транзакции

1. Нажмите кнопку редактирования (✏️) рядом с транзакцией
2. Внесите изменения
3. Нажмите "Сохранить"

### Удаление транзакции

1. Нажмите кнопку удаления (🗑️) рядом с транзакцией
2. Подтвердите удаление

### Просмотр отчетов

1. Нажмите кнопку "📊 Отчеты"
2. Выберите период (начальная и конечная дата)
3. Нажмите "Применить"
4. Просмотрите статистику за выбранный период

## Хранение данных

Данные сохраняются автоматически в JSON файл по пути:
```
%AppData%\BudgetApp\data.json
```

## Технологии

- **C#** - основной язык программирования
- **WPF** - для создания пользовательского интерфейса
- **Newtonsoft.Json** - для сериализации данных

## Лицензия

Этот проект создан в учебных целях.

