<Window x:Class="BudgetApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление бюджетом - Полнофункциональное приложение" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок и статистика -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Доходы" Foreground="White" FontSize="12" Opacity="0.8"/>
                    <TextBlock x:Name="TotalIncomeText" Text="0 ₽" Foreground="#4ECDC4" FontSize="24" FontWeight="Bold"/>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Расходы" Foreground="White" FontSize="12" Opacity="0.8"/>
                    <TextBlock x:Name="TotalExpenseText" Text="0 ₽" Foreground="#FF6B6B" FontSize="24" FontWeight="Bold"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Баланс" Foreground="White" FontSize="12" Opacity="0.8"/>
                    <TextBlock x:Name="BalanceText" Text="0 ₽" Foreground="White" FontSize="24" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Основной контент -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Список транзакций -->
            <Border Grid.Column="0" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Первая строка: основные кнопки -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                            <Button x:Name="AddTransactionButton" Content="+ Добавить транзакцию" Click="AddTransaction_Click" 
                                    Background="#4ECDC4" Foreground="White" FontWeight="Bold"
                                    ToolTip="Добавить новую транзакцию (доход или расход)"
                                    Margin="0,0,5,0"/>
                            <Button x:Name="ReportsButton" Content="📊 Отчеты" Click="ShowReports_Click" 
                                    Background="#95E1D3" Foreground="White"
                                    ToolTip="Просмотр детальных отчетов и статистики"
                                    Margin="0,0,5,0"/>
                            <Button x:Name="ManageCategoriesButton" Content="📁 Категории" Click="ManageCategories_Click" 
                                    Background="#AA96DA" Foreground="White"
                                    ToolTip="Управление категориями доходов и расходов"
                                    Margin="0,0,5,0"/>
                            <Button x:Name="ManageLimitsButton" Content="💰 Лимиты" Click="ManageLimits_Click" 
                                    Background="#FFA07A" Foreground="White"
                                    ToolTip="Установка лимитов бюджета по категориям"
                                    Margin="0,0,5,0"/>
                            <Button x:Name="ExportButton" Content="💾 Экспорт" Click="ExportData_Click" 
                                    Background="#F38181" Foreground="White"
                                    ToolTip="Экспорт данных в CSV файл"
                                    Margin="0,0,5,0"/>
                        </StackPanel>
                        
                        <!-- Вторая строка: фильтры и поиск -->
                        <StackPanel Grid.Row="1" Orientation="Horizontal">
                            <ComboBox x:Name="FilterComboBox" Width="150" SelectionChanged="FilterComboBox_SelectionChanged" Margin="0,0,5,0">
                                <ComboBoxItem Content="Все" IsSelected="True"/>
                                <ComboBoxItem Content="Доходы"/>
                                <ComboBoxItem Content="Расходы"/>
                            </ComboBox>
                            <ComboBox x:Name="CategoryFilterComboBox" Width="150" SelectionChanged="CategoryFilterComboBox_SelectionChanged" Margin="0,0,5,0">
                                <ComboBoxItem Content="Все категории" IsSelected="True"/>
                            </ComboBox>
                            <TextBox x:Name="SearchTextBox" Width="200" 
                                     TextChanged="SearchTextBox_TextChanged" 
                                     GotFocus="SearchTextBox_GotFocus"
                                     LostFocus="SearchTextBox_LostFocus"/>
                        </StackPanel>
                    </Grid>

                    <DataGrid x:Name="TransactionsGrid" Grid.Row="1" 
                              AutoGenerateColumns="False" 
                              CanUserAddRows="False"
                              SelectionMode="Single"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              Margin="10">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Дата" Binding="{Binding Date, StringFormat='{}{0:dd.MM.yyyy}'}" Width="100"/>
                            <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*"/>
                            <DataGridTextColumn Header="Категория" Binding="{Binding Category}" Width="150"/>
                            <DataGridTextColumn Header="Сумма" Binding="{Binding Amount, StringFormat='{}{0:N2} ₽'}" Width="120">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Type}" Value="Income">
                                                <Setter Property="Foreground" Value="#4ECDC4"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Type}" Value="Expense">
                                                <Setter Property="Foreground" Value="#FF6B6B"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTemplateColumn x:Name="ActionsColumn" Header="Действия" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="✏️" Click="EditTransaction_Click" 
                                                    Tag="{Binding Id}" Background="Transparent" BorderThickness="0" Padding="5"/>
                                            <Button Content="🗑️" Click="DeleteTransaction_Click" 
                                                    Tag="{Binding Id}" Background="Transparent" BorderThickness="0" Padding="5"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Статистика по категориям -->
            <Border Grid.Column="1" Background="#F8F9FA" Padding="10">
                <ScrollViewer>
                    <StackPanel>
                        <TextBlock Text="Расходы по категориям" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                        <ItemsControl x:Name="ExpenseCategoriesList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White" Margin="0,0,0,5" Padding="10" CornerRadius="5">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Rectangle Grid.Column="0" Width="15" Height="15" 
                                                   Fill="{Binding Color}" Margin="0,0,10,0"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Key}" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="2" Text="{Binding Value, StringFormat='{}{0:N2} ₽'}" 
                                                   FontWeight="Bold" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Text="Доходы по категориям" FontSize="16" FontWeight="Bold" Margin="0,20,0,10"/>
                    <ItemsControl x:Name="IncomeCategoriesList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White" Margin="0,0,0,5" Padding="10" CornerRadius="5">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Rectangle Grid.Column="0" Width="15" Height="15" 
                                                   Fill="{Binding Color}" Margin="0,0,10,0"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Key}" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="2" Text="{Binding Value, StringFormat='{}{0:N2} ₽'}" 
                                                   FontWeight="Bold" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Window>

